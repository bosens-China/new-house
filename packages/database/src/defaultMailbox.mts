import { Data } from '@new-house/database/model/mail';

const defaultMailbox: Array<Data> = [
  {
    mailbox: '1123598783@qq.com',
    disable: false,
    ceilingPrice: 2200000,
    createdDate: new Date().valueOf(),
  },
];

export default defaultMailbox;
